= form_with(model: bed, local: true) do |form|
  - if bed.errors.any?
    #error_explanation
      %h2
        = pluralize(bed.errors.count, "error")
        prohibited this bed from being saved:
      %ul
        - bed.errors.full_messages.each do |message|
          %li= message

  .field
    = form.label :available
    = form.text_field :available, id: :bed_available

  .field
    = form.label :reservation_id
    = form.text_field :reservation_id, id: :reservation_id

  .actions
    = form.submit "Submit"
