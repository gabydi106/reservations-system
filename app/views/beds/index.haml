%p#notice= notice
%h1 Beds Available
%table
  %thead
    %tr
      %th Bed #
      %th Reservation #
      %th Checkin
      %th Checkout
      %th{:colspan => "3"}
  %tbody
    - @beds.each do |bed|
      %tr
        %td= bed.id
        %td= link_to bed.reservation.id, reservation_url(bed.reservation.id)
        %td= bed.reservation.checkin
        %td= bed.reservation.checkout
        %td= link_to 'Edit', edit_bed_path(bed)
%br/
